import SlideData from "../types"

export const crateSlideDataPartition = (slideList: Array<SlideData.Slide>) =>{
    let textList = Array()
    let imageList = Array()

    slideList.forEach( slide => {
        slide.objects
    })
}